image: docker:latest

stages:
- build
- deploy

build-docker:
  services:
  - docker:latest

  before_script:
  - docker login -u $CI_REGISTRY_USER -p $CI_REGISTRY_PASSWORD
  - docker build -t sendmessages . 

  stage: build
  tags: 
    - docker-runner
  script:
  - echo "Teste"